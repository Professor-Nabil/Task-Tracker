```bash
npm install -D vitest --verbose
```

Add this to your package.json scripts:

```json
"scripts": {
  "test": "vitest",
  "test:ui": "vitest --ui"
}
```

Now, run it in your terminal:

```bash
npm test
```
