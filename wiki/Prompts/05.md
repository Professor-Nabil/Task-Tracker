Here is `./src/controllers/controller-add.js`

```js
import { serviceAddTask } from "../services/service-add-task.js";
import { printSuccessOperition } from "../views/print-success-operation.js";
export const controllerAdd = async (desc) => {
  try {
    const result = await serviceAddTask(desc);
    printSuccessOperition(result, "Task added!");
  } catch (error) {
    console.error(error);
  }
};
```

Here is `./src/views/print-success-operation.js`

```js
export const printSuccessOperition = (task, msg) => {
  console.log(`\x1b[32m✔ Success:\x1b[0m ${msg}`);
  console.log(`ID:   ${task.id}`);
  console.log(`Desc: ${task.description}`);
  console.log(`Status: ${task.status}`);
  console.log(`----------------------------`);
};
```

wait we already stated or still not started yet
