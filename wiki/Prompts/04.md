Here is `./bin/task-cli.js`

```js
#!/usr/bin/env node
import "../src/app.js";
```

Here is `./src/app.js`

```js
import { routesCommandsInputs } from "./routes/router-commands-inputs.js";
import { validateArgvLength } from "./middlewares/validate-argv-length.js";
const cliInputs = process.argv.slice(2);
validateArgvLength(cliInputs);
const run = async () => {
  try {
    await routesCommandsInputs(cliInputs);
    process.exit(0);
  } catch (error) {
    console.error("CRITICAL SYSTEM ERROR:", error.message);
    process.exit(1);
  }
};
run();
```

Here is `./src/routes/router-commands-inputs.js`

```js
import { validateDescription } from "../middlewares/validate-description.js";
import { validateId } from "../middlewares/validate-id.js";
import { validateStatus } from "../middlewares/validate-status.js";
import { controllerAdd } from "../controllers/controller-add.js";
import { controllerUpdate } from "../controllers/controller-update.js";
import { controllerDelete } from "../controllers/controller-delete.js";
import { controllerMark } from "../controllers/controller-mark.js";
import { controllerList } from "../controllers/controller-list.js";
import { controllerHelp } from "../controllers/controller-help.js";

export const routesCommandsInputs = async (cliInputs) => {
  try {
    const [arg0, arg1, arg2] = cliInputs;
    const commands = {
      add: async () => {
        const desc = arg1;
        await validateDescription(desc);
        await controllerAdd(desc);
      },
      update: async () => {
        const id = arg1;
        const desc = arg2;
        await validateId(id);
        await validateDescription(desc);
        await controllerUpdate(id, desc);
      },
      delete: async () => {
        const id = arg1;
        await validateId(id);
        await controllerDelete(id);
      },
      mark: async () => {
        const id = arg1;
        const status = arg2;
        await validateId(id);
        await validateStatus(status);
        await controllerMark(id, status);
      },
      list: async () => {
        const filter = arg1;
        await controllerList(filter);
      },
    };
    if (commands[arg0]) {
      await commands[arg0]();
    } else {
      await controllerHelp(cliInputs);
    }
  } catch (error) {}
};
```

User inputs outputs

```bash
~/Github_Nabil_Tester/Task-Tracker (MVC ✗) node src/app.js add "Task 1"
✔ Success: Task added!
ID:   1
Desc: Task 1
Status: not-done
----------------------------
```

```bash
~/Github_Nabil_Tester/Task-Tracker (MVC ✗) task-cli add "Task 2"
✔ Success: Task added!
ID:   2
Desc: Task 2
Status: not-done
----------------------------
```

```bash
~/Github_Nabil_Tester/Task-Tracker (MVC ✗) npm start add "Task 3"

> task-tracker-cli@1.0.0 start
> node src/app.js add Task 3

✔ Success: Task added!
ID:   3
Desc: Task 3
Status: not-done
----------------------------
```

Please if you need to know any thing tell me
